{
  "version": 2,
  "buildCommand": "prisma generate && next build",
  "env": {
    "NODE_ENV": "production"
  },
  "crons": [
    {
      "path": "/api/rituals/check",
      "schedule": "0 9 * * 5"
    }
  ]
}
