{
  "version": 2,
  "buildCommand": "prisma generate && next build",
  "env": {
    "NODE_ENV": "production"
  },
  "crons": [
    {
      "path": "/api/rituals/run",
      "schedule": "30 8 * * *"
    }
  ]
}
